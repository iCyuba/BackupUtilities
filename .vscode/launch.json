{
    "version": "0.2.0",
    "configurations": [
        {
            "name": ".NET Core Launch: BackupUtility",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "build",
            "program": "${workspaceFolder}/bin/Debug/net8.0/BackupUtility.dll",
            "args": [
                "--once",
                "--config",
                "${workspaceFolder}/config/config.json"
            ]
        }
    ]
}
